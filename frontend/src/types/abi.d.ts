// Type definitions for contract ABIs
declare module '*/abis/LendingEscrow.json' {
  import { ContractInterface } from 'ethers';
  const abi: ContractInterface;
  export = { abi };
}

declare module '*/abis/SubscriptionToken.json' {
  import { ContractInterface } from 'ethers';
  const abi: ContractInterface;
  export = { abi };
}
